spring:
  profiles:
    active: default
    include:
      - mysql
      - redis
      - jwt
      - flyway
      - oauth
      - jasypt

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  level:
    com.example : DEBUG